[project]
name = "ghost"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
generate = [
    "wordfreq>=3.1.1",
]
